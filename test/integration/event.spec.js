describe('events', () => {
  describe('app_mention', () => {
    it('creates new list with members of channel in list', () => {

    })

    it('does not create new list of list already associated with channel', () => {

    })
  })
})